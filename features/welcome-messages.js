// Welcome messages functionality
// This is mostly handled in the guildMemberAdd and guildMemberRemove events
const setupWelcomeMessages = (client) => {
  // No additional setup needed as the events handle this
  console.log("Welcome messages feature initialized")
}

module.exports = {
  setupWelcomeMessages,
}
