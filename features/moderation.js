// Moderation functionality
// Most of this is handled in the messageCreate event and slash commands
const setupModeration = (client) => {
  // No additional setup needed as the events and commands handle this
  console.log("Moderation feature initialized")
}

module.exports = {
  setupModeration,
}
